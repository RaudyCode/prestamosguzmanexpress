extends ../layout/index.pug
block contenido

   div(class="flex flex-col justify-center items-center py-5")
        div(class="max-w-96 py-5 mb-10")
            img(src="/img/logo.svg", alt="Logo Presta G")
        h2.text-center.text-2xl.font-extrabold.mb-4= pagina
        div(class="bg-white shadow py-8 px-4 rounded-lg max-w-md mx-auto")
            p(class=(error ? 'bg-red-500' : 'bg-green-500') + ' py-2 px-5 rounded-lg w-full font-bold text-center text-white uppercase')= mensaje

            
            a(href="/rutas" class="text-center font-bold text-sm bg-green-600 hover:bg-green-500 text-white uppercase mt-4 p-2 block") Volver a Rutas Admin
